/**
 * التحقق من صيغة country_code — عمداً بلا قائمة ثابتة (لا يشبه DOMAIN_KEYS).
 * الدول مرجع بيانات (جدول countries) لا مفردات منطق أعمال: إضافة دولة جديدة
 * تصبح INSERT فى الجدول فقط، بلا أى تعديل كود أو نشر جديد. التحقق هنا صيغى
 * فقط (حرفان كبيران — ISO 3166-1 alpha-2)؛ صحة القيمة فعلياً (هل الدولة
 * موجودة؟) تُترك لقيد REFERENCES countries(code) فى قاعدة البيانات.
 */
export const COUNTRY_CODE_PATTERN = /^[A-Z]{2}$/;
